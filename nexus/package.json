{
  "name": "nexus",
  "version": "0.1.0",
  "private": true,
  "description": "Action to generate maven settings.xml",
  "main": "lib/main.js",
  "scripts": {
    "clean": "rm -rf lib/ dist/",
    "build": "tsc",
    "package": "ncc build --source-map --license licenses.txt",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/io": "^1.1.1",
    "ts-dedent": "^2.2.0",
    "xmlbuilder2": "^3.0.2",
    "yaml": "^1.10.2"
  }
}
